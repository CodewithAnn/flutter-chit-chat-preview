import 'package:flutter/material.dart';

const lightNavy = Color(0xff5271ff); // main color light mode
const blueWhite = Color(0xffCDD6FF); // use with lightNavy for gradient.
const lightBlue = Color(0xff7DBEFF); // main color in dark mode.
const transBlue = Color.fromARGB(255, 113, 180, 247);
const vividNavy = Color(0xff123CFF); // accent color
const baseNavy = Color(0xff91A5FF); // base color
const onBackground = Colors.white;
const darkGrey = Color(0xff485B6F);
const white = Colors.white;
const black = Colors.black;
